{
  "name": "autogit",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "bun build ./index.ts --compile --outfile autogit --minify --sourcemap --bytecode && rm -f *.bun-build"
  },
  "dependencies": {
    "@ai-sdk/cerebras": "^1.0.15",
    "@cerebras/cerebras_cloud_sdk": "^1.50.0",
    "ai": "^5.0.41"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}

